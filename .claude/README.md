# Claude Code Compatibility Copy

This directory is a Claude Code compatibility copy.

For this repository, the Codex source of truth is:

- `AGENTS.md`
- `shared/harness.yaml`
- `shared/institution-profile.yaml`
- `shared/references/permission-model.yaml`

If Claude Code is maintained in a separate harness repository, treat this directory as compatibility/reference material only. It is not the center of the Codex harness.
